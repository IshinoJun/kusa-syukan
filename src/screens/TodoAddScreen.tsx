import React from 'react';
import { View, Text } from 'react-native';

const TodoAddScreen = (): JSX.Element => {
  return (
    <View>
      <Text>TodoAddScreen</Text>
    </View>
  );
};

export default TodoAddScreen;
