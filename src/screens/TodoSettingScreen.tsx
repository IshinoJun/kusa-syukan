import React from 'react';
import { View, Text } from 'react-native';

const TodoSettingScreen = (): JSX.Element => {
  return (
    <View>
      <Text>TodoSettingScreen</Text>
    </View>
  );
};

export default TodoSettingScreen;
